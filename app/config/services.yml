# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
#    service_name:
#        class: AppBundle\Directory\ClassName
#        arguments: ["@another_service_name", "plain_value", "%parameter_name%"]

    guzzle:
        class: GuzzleHttp\Client

    token_storage_session:
        class: OAuth\Common\Storage\Session

    withings_api_adapter:
        class: AppBundle\ApiAdapter\WithingsApiAdapter
        arguments: ["%withings_consumer_key%", "%withings_consumer_secret%", "%withings_callback_uri%", "@token_storage_session"]

    user_data:
        class: AppBundle\UserData\UserData
        arguments: ["@database_connection"]

    entity_provider:
        class: AppBundle\Entity\Provider
        arguments: ["@database_connection"]