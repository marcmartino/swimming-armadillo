[
{% for event in measurement_events %}
    {
    "Date":"{{ event.time }}",
    {#"Weight (lb)":{{ event.weight }},#}
    {#"Fat mass (%)":{{ event.fatmass }},#}
    {#"Lean mass (%)":{{ event.leanmass }},#}
    "Units":"{{ event.units }}"
    {#"Comments":""#}
    }{% if loop.last %}{% else %},{% endif %}
{% endfor %}
]